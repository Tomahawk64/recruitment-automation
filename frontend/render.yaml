# Render.com Build Configuration
# This file tells Render how to build the frontend

# Build Command (set in Render dashboard)
# npm install && npm run build

# Publish Directory (set in Render dashboard)  
# frontend/dist

# Environment Variables (set in Render dashboard)
# VITE_API_URL=http://13.61.100.205:4000

# Auto-deploy on push to main branch: YES
